defmodule Practice.Factor do
  def factor(num) do
    # divide num by first prime (2) until there is a decimal/remainder
    # then divide by next prime until all that is left are primes
    "factor"
  end
end
